[package]
name = "server"
version = "0.1.0"
authors = ["quininer <quininer@live.com>"]
edition = "2018"

[dependencies]
structopt = "0.2"
async-tls = { path = "../.." }
async-std = { path = "../../../async-std" }
rustls = "0.16"
webpki = "0.21"

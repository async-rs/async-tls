[package]
name = "client"
version = "0.5.0"
authors = ["The async-rs authors", "quininer <quininer@live.com>"]
edition = "2018"

[dependencies]
structopt = "0.2"
rustls    = "0.16"
async-std = "0.99"
async-tls = { path = "../.." }
